# Generated/derived files — outputs of the pipeline, not source
governance/*.csv


# Keep folder structure but ignore contents (remove the two lines above
# instead if you WANT the sample CSVs committed so others can run this
# without hunting for the data)

# Python
__pycache__/
*.pyc
venv/
.venv/

# Environment / secrets
.env

# OS/editor cruft
.DS_Store
Thumbs.db
.vscode/settings.json

# Power BI — keep the .pbix, ignore local cache/lock files
*.pbix.lock
